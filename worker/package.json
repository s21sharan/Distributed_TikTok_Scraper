{
  "name": "tiktok-worker",
  "version": "1.0.0",
  "description": "Standalone TikTok scraper worker",
  "main": "worker.js",
  "scripts": {
    "start": "node start.js",
    "worker": "node worker.js",
    "dev": "nodemon worker.js",
    "install-deps": "npm install && pip3 install -r requirements.txt"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "ioredis": "^5.3.2",
    "node-cron": "^3.0.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 